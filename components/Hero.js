import React from "react";
import Image from "next/image";
import { Button } from "./ui/button";
import { fadeIn } from "@/variants";

const Hero = () => {
  return <section className="bg-hero">Hero</section>;
};

export default Hero;
